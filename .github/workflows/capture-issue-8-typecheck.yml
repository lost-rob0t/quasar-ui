name: Capture issue 8 typecheck

on:
  push:
    branches:
      - agent/issue-8-command-engine
    paths:
      - .github/workflows/capture-issue-8-typecheck.yml

permissions:
  contents: write

jobs:
  capture:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: agent/issue-8-command-engine
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Capture compiler diagnostics
        run: npm run typecheck > issue-8-typecheck.log 2>&1 || true
      - name: Commit diagnostics
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add issue-8-typecheck.log
          git commit -m "capture issue 8 typecheck diagnostics"
          git push origin HEAD:agent/issue-8-command-engine
